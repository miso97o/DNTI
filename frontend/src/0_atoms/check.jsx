function checkBus(el) {
  if(el.value === 'bus') {
    return true
  }
}
function checkSubway(el) {
  if(el.value === 'subway') {
    return true
  }
}
function checkBike(el) {
  if(el.value === 'bike') {
    return true
  }
}
function checkOliveyoung(el) {
  if(el.value === 'oliveyoung') {
    return true
  }
}
function checkDaiso(el) {
  if(el.value === 'daiso') {
    return true
  }
}
function checkMart(el) {
  if(el.value === 'mart') {
    return true
  }
}
function checkCVS(el) {
  if(el.value === 'cvs') {
    return true
  }
}
function checkPark(el) {
  if(el.value === 'park') {
    return true
  }
}
function checkGym(el) {
  if(el.value === 'gym') {
    return true
  }
}
function checkRestaurant(el) {
  if(el.value === 'restaurant') {
    return true
  }
}


export { checkBus, checkSubway, checkBike, checkOliveyoung, checkDaiso, checkMart, checkCVS, checkPark, checkGym, checkRestaurant }